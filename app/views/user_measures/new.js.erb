function displayMeasures(content) {
  const target = document.getElementById('user_measure_form');
  target.innerHTML = content;
};
displayMeasures('<%= j render "user_measures/form", measures: @measures %>');
